# Deuda técnica y plan de mejora — mw-front

Checklist accionable generado a partir de una auditoría completa del proyecto (dependencias, arquitectura, seguridad, rendimiento, accesibilidad/i18n). Ordenado por prioridad. Marcar `[x]` a medida que se resuelve cada punto.

## 🔴 Crítico

- [x] **Purgar archivos de usuarios reales versionados en `public/assets/`** (`users/ramiroromero.png`, `pictures/file_*-<timestamp>.png/pdf`) — se sirven sin login en cada build y están en el historial de git. *(Resuelto con `git rm` simple: quedan fuera de commits futuros. No se reescribió el historial pasado — sigue visible en commits viejos si alguien lo busca; evaluar `git filter-repo` más adelante si se necesita cerrarlo del todo. De paso se verificó que `users/user.png`, `users/usuario.png`, `pictures/usuario.png` y `pictures/profiles/362c...png` son el mismo placeholder genérico byte-a-byte — esos quedan, no son datos reales.)*
- [x] **Destrackear `.env.hiper`, `.env.provasa`, `.env.test`** — están en git pese a que `.gitignore` los excluye desde antes. *(Resuelto con `git rm --cached`; los archivos siguen en disco para los builds locales por marca, mismo cuidado sobre historial pasado que el punto anterior.)*
- [x] **Arreglar bug de logout en `src/helpers/core.js`** — `logoutUser()` se llama sin `dispatch`, no hace nada al store. Al expirar el token, el usuario queda "logueado" en Redux viendo errores repetidos en vez de ser redirigido a `/login`. *(Resuelto: se agregó `getStore()` en `src/redux/stores.js` para poder despachar fuera de React, y `core.js` ahora despacha `logoutUser` con un navigate normalizado a `window.location.hash`. Verificado en navegador: con token expirado, redirige a `#/login` y limpia `localStorage`.)*
- [x] **Agregar Error Boundary global** en `src/main.js`/`src/App.js` — hoy cualquier excepción de render deja la pantalla en blanco sin aviso. *(Resuelto: nuevo `src/components/ErrorBoundary/ErrorBoundary.jsx` envolviendo `<Provider><App/></Provider>` en `main.js`. Verificado forzando una excepción de render: muestra pantalla de fallback con botón "Recargar" en vez de blanco.)*
- [x] **Introducir framework de tests** (Vitest, ya usan Vite) — 0 tests hoy en un ERP que calcula nómina/impuestos/contabilidad. Empezar por helpers de cálculo críticos. *(Resuelto: Vitest configurado en `vite.config.js` (`test.environment: 'jsdom'`, reutiliza los alias `@Helpers`/etc.), scripts `npm test`/`npm run test:watch`. Primeros 21 tests en `src/helpers/DateHelper.test.js` y `src/helpers/Utils.test.js`, todos pasando. Queda como base — falta cobertura de los helpers de nómina/impuestos y de `core.js`.)*
- [x] **Aislar o reemplazar `xlsx`** (vulnerabilidad "high" sin fix, prototype pollution/ReDoS) — usada en `src/helpers/Utils.js`, importada de forma estática (también infla el bundle, ver más abajo). *(Parcial: se aisló con `import()` dinámico en `getExcelData` — ya no viaja en el chunk de entrada (bajó de 1058 KB a 723 KB, gzip 310→197 KB), ahora es su propio chunk de 429 KB que solo carga al usar la importación de asistencia. La vulnerabilidad en sí sigue sin fix upstream; reemplazar la librería queda pendiente si se necesita cerrar el hallazgo por completo.)*

## 🟠 Medio

**Redux / estado**
- [x] Borrar código Redux muerto: `src/redux/actions/`, `src/redux/reducers/` (duplicados sin uso), `src/redux/auth/mwReducer.js`, y `src/AppProvider.js`/`src/AppContext.js`/`src/AppRenderer.js` (implementación de auth paralela, rota, sin uso). *(Resuelto: verificado con grep que nada los importaba por ruta explícita, borrados, y confirmado con `vite build` + `npm test` + chequeo en navegador que la app sigue funcionando igual.)*
- [x] Evaluar migrar el estado restante a Redux Toolkit (`createSlice`/`createAsyncThunk`) y eliminar `redux-saga` (hoy se usa para un solo saga de auth). *(Parcial: los 4 reducers (auth, menu, settings, generalData) migrados a `createSlice`, y `stores.js` usa `configureStore` de RTK en vez de `createStore`/`combineReducers`/`applyMiddleware` manual. Se mantuvieron los mismos action types (constants de `contants.js`) vía `extraReducers`, así que ningún action creator ni call site cambió — cero riesgo para el resto de la app. Se borró también `src/redux/store/index.js` (código muerto, importaba `connected-react-router`/`history`, paquetes ni siquiera instalados). Verificado en navegador: dashboard, toggle de menú, cambio de idioma EN↔ES, y el flujo de logout por token expirado (saga + slice) funcionan igual que antes. **No se eliminó `redux-saga`** — migrar login/forgot/reset a `createAsyncThunk` es el siguiente paso lógico pero requiere probar el login real contra un backend, que no está disponible en este entorno; queda pendiente.)*

**Cliente HTTP (`src/helpers/core.js`)**
- [x] Revisar `response.ok`/status HTTP en cada método (GET/POST/PUT/DELETE/getJSON/uploadFiles). *(Resuelto vía helper `parseResponse`: si el HTTP status no es 2xx, se arma un objeto de error con forma consistente `{status, statusCode, messages}` en vez de dejar que `response.json()` explote sin control sobre bodies no-JSON.)*
- [x] Agregar timeout/cancelación (`AbortController`). *(Resuelto vía helper `fetchWithTimeout`: 30s por defecto, 120s para `uploadFiles`. Antes un backend que nunca respondía dejaba el loading prendido para siempre.)*
- [x] Invocar `fnFinally` realmente (hoy se recibe pero nunca se llama; cada hook duplica el apagado de `loading`). *(Resuelto: se agregó `.finally()` a GET/POST/PUT/DELETE. Ningún caller pasa `fnFinally` todavía, así que el efecto práctico se ve recién cuando se refactoricen los hooks para usarlo — queda como base.)*
- [x] Centralizar construcción de query strings con `URLSearchParams`/`encodeURIComponent` (hoy concatenación manual en ~156 archivos). *(Resuelto: nuevo helper `buildUrl(path, params)` en `core.js` (`URLSearchParams` internamente, filtra `undefined`/`null`, preserva string vacío). Se auditó el alcance real con grep: 143 call sites en 86 archivos, no 156. De esos, ~130 solo interpolaban IDs numéricos (riesgo bajo) y 13 (todos en `hotelManagement`) interpolaban texto de búsqueda libre sin encodear — el bug real y concreto que motivó este ítem (un usuario escribiendo "&"/"=" en un buscador rompía o contaminaba la query string). Se optó por el alcance completo (143/143) para consistencia total, no solo los 13 de riesgo real.
  - **Proceso**: se escribió un codemod (Node, no versionado, corrido una sola vez) que detectó los 143 template literals `` request.MÉTODO(\`path?a=${a}&b=${b}\` `` vía regex, verificó que las 62 expresiones distintas usadas fueran simples (sin ternarios/`&&`/`||`/nested braces) antes de tocar nada, y reemplazó cada uno por `request.MÉTODO(buildUrl('path', { a, b }))`, agregando `buildUrl` al import de `core` existente en cada archivo.
  - **Bug real durante el proceso**: la primera pasada del codemod tenía un paréntesis de cierre de más en el string de reemplazo, lo que cortaba `request.MÉTODO(...)` antes de tiempo y dejaba los callbacks (`fnSuccess`, `fnError`...) colgando como una expresión suelta — **se detectó con `vite build` fallando y se revirtieron los 86 archivos con `git checkout` antes de commitear nada**, se corrigió el codemod, y se reaplicó.
  - **Verificación**: comparación automatizada de conteo de errores de lint (antes vía `git show HEAD:archivo` vs después) en los 86 archivos — confirmó exactamente 1 regresión real (un import de `buildUrl` sin usar porque el único call site en ese archivo estaba dentro de una línea comentada) y 0 en el resto; se corrigió esa 1 a mano. Más: build + `npm test` limpios, revisión manual de varios diffs representativos, y verificación end-to-end en navegador capturando la URL real de red (`admin/documents/?useBill=1`, idéntica a la original para el caso "normal"). Nuevos tests unitarios de `buildUrl` (5, en `core.test.js`) cubren específicamente el encoding de `&`, `=`, espacios y acentos en texto de búsqueda.)*
- [x] Decidir sobre `src/helpers/coreOld.js` (versión mejor pero sin uso) — terminar la migración o borrarlo. *(Ya resuelto arriba, en la sección de limpieza de código muerto.)*
- [x] `GETPdf`, `GETPdfUrl`, `getFile` y `fnExportToXLSX` (descargas por blob/XHR) no recibieron timeout ni el manejo de `response.ok` — quedan con el comportamiento original, pendiente si se necesita. *(Resuelto para `GETPdf`/`GETPdfUrl`/`getFile`: mismo patrón `fetchWithTimeout` + nuevo helper `parseBlobResponse` (revisa `response.ok`, intenta parsear el body como JSON de error si falla). De paso se arregló un bug preexistente en `GETPdf`: el segundo `.then` llamaba `response.blob()` sobre algo que ya era un blob (no un `Response`), lo que tiraba una excepción silenciosa después de cada descarga exitosa y disparaba `fnError` erróneamente. `fnExportToXLSX` sigue con XHR crudo sin tocar — usa un mecanismo distinto (progreso de subida/descarga vía XHR) y tocarlo es un cambio de mayor alcance, no incluido acá.)*
- [x] Falta cobertura de tests para `core.js` en sí (requiere mockear `fetch`/Redux store/`localStorage`) — quedó fuera de este pase. *(Resuelto: nuevo `src/helpers/core.test.js`, 10 tests mockeando `fetch`/`@Redux/stores`/`@Containers/ui/Notifications` vía `vi.mock`. Cubre: éxito, error HTTP con body no-JSON, fallo de red, `fnFinally` siempre invocado (regresión del bug original), token ausente/expirado dispara logout, URLs públicas no chequean token. Se agregó un bloque de `globals.node` en `eslint.config.js` para archivos `*.test.js` — usan `Buffer`/`global` aunque corren bajo `test.environment: 'jsdom'`.)*

**Duplicación de patrones de UI**
- [x] Extraer `useTableConf` repetido en `settings/*` a un hook compartido `buildTableConfig(...)`. *(Resuelto: nuevo `src/hooks/useTableConfig.js` (named export, re-exportado desde `@Hooks`), consumido por `billingAreas/ModalViewDocuments.jsx`, `cashBoxes/Content.js` y `discounts/Content.js`. Borrados los 3 `useTableConf.js` duplicados. Verificado en navegador: las 3 pantallas renderizan sus tablas con columnas/acciones/traducciones correctas.)*
- [x] Extraer `fnExportToXLSX` (17 implementaciones) a un hook `useExportExcel`. *(Resuelto: nuevo `src/hooks/useExportExcel.js`, consumido en los 17 call sites. De paso corrige un bug real que tenían los 17: como no había try/catch, si la exportación fallaba la promesa quedaba rechazada sin manejar y `setLoading(false)` nunca se ejecutaba (loader trabado para siempre); ahora el hook usa try/catch/finally y muestra un toast de error. Verificado con build + tests + navegador en 4 pantallas, sin errores nuevos.)*
- [x] Unificar contrato de "select" (`{value,label}` vs `{id,name}`) entre `SearchSelect` y `SimpleSelect`. *(Resuelto con un enfoque distinto al planteado originalmente — investigado antes de tocar nada: `SearchSelect` domina con 136 usos (siempre requiere mapear, el backend nunca devuelve `{value,label}` directo) vs 54 de `SimpleSelect` (frecuentemente recibe `resp.data` crudo sin mapear, porque coincide con `{id,name}`). De los 32 archivos que usan ambos, no se encontró evidencia de que la *misma* lista se mapee dos veces — son listas distintas por campo, no doble trabajo real sobre el mismo dato. Forzar un shape único a los ~190 call sites le habría agregado mapeo donde hoy no hace falta, sin ahorrar nada real.
  En vez de eso: se agregaron props opcionales `getOptionValue`/`getOptionLabel` a ambos componentes (mismo patrón que ya expone `react-select` nativamente), con defaults que preservan el comportamiento exacto de siempre (`.value`/`.label` en `SearchSelect`, `.id`/`.name` en `SimpleSelect`). **Cero cambios en los ~190 call sites existentes** — retrocompatible. De ahora en más, pantallas nuevas pueden pasarle a cualquiera de los dos componentes cualquier shape de datos sin mapear, especificando el accessor que corresponda.
  Verificado con build + lint + `npm test` + navegador: una pantalla con ambos componentes en el mismo formulario (`humanResources/process/accidents`) renderiza y abre el menú de `SearchSelect` sin errores nuevos.)*
- [x] Evaluar consolidar duplicidades de librería: tablas (`XReactTable` / `ReactTableEdit` / `SimpleTable`), calendarios (`react-big-calendar` + FullCalendar), datepickers (`react-datepicker` + `react-datetime`), dropzone (`react-dropzone` + `react-dropzone-component`). *(Resuelto para datepicker/dropzone/calendario/iconos — tablas queda pendiente, ver abajo:*
  - ***Datepickers**: los 4 usos reales de `DateTimeCalendar` (react-datetime + moment) migrados a react-datepicker con `showTimeSelect` (mismo output `"YYYY-MM-DD HH:mm:ss"`, cero cambios en los call sites). `react-datepicker` ya era la librería dominante (69 usos vía `dateCalendar`). Se descubrió y arregló de paso un bug real: `uploadFile/index.js` llamaba al hook `IntlMessages` dentro de un `useEffect` (viola Rules of Hooks), causando un crash real en las 4 pantallas recién verificadas — no relacionado a esta tarea, pero se coló en el commit de i18n de antes en la sesión.*
  - ***Dropzone**: `react-dropzone-component` (`src/components/dropzone/`) resultó ser código muerto — su único consumidor (`views/app/production/...`) pertenece a un módulo entero comentado/sin ruta en `App.js`. Borrado junto con el paquete; `react-dropzone` (hooks, usado por `UploadImages.jsx`) queda como única librería de dropzone real.*
  - ***Calendario**: `react-big-calendar` (`views/app/start/start.jsx`) también resultó ser código muerto (sin ruta, sin importadores vivos) — no era una duplicación real en la app corriendo. Se sacó el paquete; FullCalendar queda como la única librería de calendario en uso. `start.jsx`/`ModalViewProject.jsx` quedan con un import colgante, mismo tratamiento que el módulo `production`.*
  - ***Iconos**: `@fortawesome/react-fontawesome` tenía un solo import, y ni siquiera se usaba dentro del archivo (`CardHotel.jsx` usa clases `bi bi-*` de bootstrap-icons). Import muerto eliminado, paquete desinstalado.*
  - ***Datepicker duplicado sin uso**: `src/components/datePicker/` (otro wrapper de react-datepicker, distinto de `dateCalendar`) tenía 0 consumidores — borrado.*
  - *Verificado con build + npm test + navegador en las 4 pantallas de datepicker (calendario + selector de hora abren correctamente, valor final con fecha y hora).)*
- [x] Refactorizar hooks monolíticos de pantalla (`useEmployees.js` 962 líneas, `usePurchases.js` 420 líneas) separando formulario/validación/llamadas API. *(Enfoque: extraer carga de listas de referencia y sub-flujos cohesivos a hooks propios, sin tocar la lógica interna de guardado (`fnSaveEmployee`/`fnSavePurchase`) — no hay backend real para verificar un guardado de punta a punta.
  - `usePurchases.js`: 419→320 líneas. Extraído `usePurchaseFormLists.js` (las 4 listas de referencia del mount effect) y `usePurchaseOrders.js` (sub-flujo de "importar desde orden de compra"). Mismo conteo de lint antes/después (18 problemas, solo redistribuidos). Verificado en navegador: los 4 GETs de referencia disparan igual que antes, botón "Load P. Orders" funciona.
  - `useEmployees.js`: 961→759 líneas. Extraído `useEmployeeReferenceData.js` (8 listas de referencia + `fnGetProjects`/`fnGetAreaManager`, que useEmployees.js sigue necesitando reinvocar). Colapsadas ~10 funciones `fnViewX`/`fnPermissions`/`fnVacations`/`fnIncapacities` casi idénticas (chequear permiso, avisar si no autorizado, abrir modal si `id>0`) en un único helper `fnGuardedOpenModal(setOpen)`. Mismo conteo de errores de lint antes/después (19), 1 warning más pero es el mismo dependency-array preexistente ahora repartido en 2 `useEffect` en vez de 1.
  - **Bug real encontrado al verificar**: `request.getFile()` (usado por las 7 pantallas que cargan una imagen de perfil/foto, incluida `useEmployees.js`) podía tirar una excepción no manejada ante un 401/404 desde que se agregó `parseBlobResponse` en un fix anterior de esta sesión — ninguno de sus 7 call sites tiene `try/catch` (antes de ese fix, `getFile` nunca rechazaba). Corregido en `core.js`: ahora atrapa el error internamente y devuelve `undefined` en vez de propagar el rechazo, preservando el contrato "nunca tira" que ya asumían los 7 callers. 2 tests nuevos en `core.test.js`. Verificado en navegador: la pantalla de empleados ahora muestra "Sin Imagen" en vez de generar un `PAGEERROR`.
  - Verificado con build + `npm test` (39 tests) + navegador en ambas pantallas.)*

**Infra / build**
- [x] Migrar el pipeline de build multi-marca (hoy depende de `post-build.js`/`vps.config.json` fuera del repo) a CI/CD versionado (ej. GitHub Actions, matrix build por marca). *(Parcial: se agregó `.github/workflows/ci.yml` (`npx vite build` + `npm test` en cada push/PR a main, `npm run lint` como paso informativo/no bloqueante dado el backlog de ~7000 hallazgos). No incluye deploy real — las credenciales FTP/SSH y el propio `post-build.js` no están en el repo, así que no hay forma de reconstruir el pipeline de deploy completo desde acá. Migrar el deploy real a CI/CD (matrix build por marca con secrets) queda pendiente y requiere que el equipo aporte esas credenciales.)*
- [ ] Resolver inconsistencia de Bootstrap: `reactstrap@9` (Bootstrap 5) pero se carga CSS de Bootstrap 4.6.2 en runtime (`src/main.js` → `assets/css/bootstrap/bootstrap.min.css`, confirmado v4.6.2 por su propio header; `bootstrap5.min.css` v5.2.3 existe vendored en la misma carpeta pero no se importa nunca). *(Investigado — el alcance real es mayor al de "cambiar un import": se cuantificó con grep que **~60 archivos** usan clases utilitarias exclusivas de Bootstrap 4, que no existen en Bootstrap 5 (se silencian sin romper el build, pero rompen visualmente espaciado/alineación): 41 archivos con `ml-`/`mr-`, 6 con `pl-`/`pr-`, 9 con `text-left`/`text-right`, 3 con `float-left`/`float-right`, y 7 con `custom-control`/`custom-checkbox`/`custom-radio` (reemplazados por `form-check`/`form-select` en BS5). Migrar esas ~60 pantallas a las clases BS5 antes de cambiar el CSS cargado, con verificación visual de cada una, queda como tarea dedicada aparte — no abordada en esta sesión por alcance/riesgo. Las 4 hojas de Bootstrap vendored sin usar sí se borraron (`bootstrap4.3.min.css`, `bootstrap5.min.css`, `bootstrap5.rtl.min.css`, `bootstrap.rtl4.3.min.css` — 784 KB), era una acción independiente y de bajo riesgo (confirmado con grep que nada las importaba). Verificado con build + npm test.)*

**i18n**
- [x] Sincronizar claves entre `en_US.js` y `es_ES.js`. *(Resuelto: se agregaron las 28 claves que faltaban en inglés, 1 que faltaba en español (`button.send`), y se corrigió un typo (`commin.price` → `common.price`) que hacía que ambos locales tuvieran esa clave con nombres distintos. Ambos locales quedan con 2901 claves idénticas. Se agregó `src/lang/locales/locales.test.js` (vía Vitest, ya que no hay CI) para que no vuelva a desincronizarse sin que falle `npm test`.)*
- [x] Migrar strings hardcodeados a `IntlMessages`/`FormattedMessage` en módulo Hotel y componentes de upload genéricos. *(Resuelto: `UploadImages.jsx`, `uploadFile/index.js`, `CardBooking.jsx` (incluyendo más strings de los originalmente detectados: "Capacidad", "Reservar", "Ver Detalle"), `HotelCheckOutToday.jsx` y `HotelOcupancyToday.jsx` (mismo caso "No Hay Datos Disponibles" repetido, no detectado antes). Claves nuevas agregadas a ambos locales, test de sincronización sigue en verde.)*

## 🟡 Bajo

- [x] Borrar código muerto: `src/App.jsx` (scaffold de create-vite), `src/AppRouter.js`, `firebaseConfig` sin uso en `defaultValues.js`. *(Resuelto: los 3 confirmados sin importadores vivos. De paso se encontró que `src/helpers/Firebase.js` (el único consumidor de `firebaseConfig`, con su import ya comentado en `saga.js`) también era código muerto — y que importaba el paquete npm `firebase`, que ni siquiera está en `package.json` como dependencia (si algo lo hubiera importado alguna vez, habría roto el build). Los 3 archivos borrados. `motion` y `bootstrap-icons` ya se habían desinstalado antes — ver auditoría de `package.json` más abajo.)*
- [x] Auditoría de `package.json`: desinstalados `bootstrap-icons`, `motion`, `redux-thunk` (redundante desde la migración a Redux Toolkit — `configureStore` ya incluye thunk) y `vite-tsconfig-paths` (devDependency, comentada y nunca activa en `vite.config.js`; se limpió también la línea de import comentada). `dotenv`, `env-var`, `archiver`, `basic-ftp`, `ssh2`, `ssh2-sftp-client`, `fs-extra` tienen 0 uso en el repo pero se dejaron a propósito — muy probablemente las usa `post-build.js` (el script de deploy gitignoreado, ausente del working tree) y no hay forma de confirmarlo sin ese archivo. Verificado con build + npm test + navegador.
- [x] Migrar los archivos que usan `moment` directo a `DateHelper` (que ya envuelve `dayjs`) y sacar `moment` del bundle. *(Completado el resto — los 11 archivos vivos que quedaban tras la pasada mecánica anterior. Solo quedan 5 archivos con `moment` en todo el repo, todos dentro de los módulos huérfanos `production/`/`start/` sin ruta ni importadores vivos (ver ítem de módulos huérfanos), fuera de alcance hasta que se decida su destino — el resto de la app ya no depende de `moment`.
  **Bug real encontrado y arreglado en `DateHelper` mismo**: `DateHelper.parse(date, format)` estaba roto para formatos no-ISO como `'DD/MM/YYYY'` — sin el plugin `customParseFormat` de dayjs, `dayjs(date, format)` ignora silenciosamente el format string y devuelve `Invalid Date` (confirmado con un script Node antes de tocar nada). Se agregó el plugin a `DateHelper.js` y un test de regresión en `DateHelper.test.js`. Sin este fix, la migración de `attendanceControl` (parsea fechas `DD/MM/YYYY` de un Excel importado) habría quedado silenciosamente rota.
  Los 11 archivos, por patrón:
  - **`moment(value).format(fmt)` → `DateHelper.format(value, fmt)`**: `SimpleTableTD.jsx`.
  - **Import muerto (el único uso estaba en código comentado)**: `Topnav.Notifications.jsx` — se borró el import sin reemplazo.
  - **`date2.diff(date1, 'days'[, true])` → `DateHelper.diff(date2, date1, 'day'[, true])`** (mismo orden de argumentos que moment: `a.diff(b)` = a − b): `ModalPermissions.jsx`, `usePermissions.js`, `ModalDetailPay.jsx` (permisos/RRHH, calculan días u horas de un permiso), `useModalNewQuote.js`, `useModalAddRes.js`, `useModalInvoice.js` (hotel, calculan noches entre check-in/check-out).
  - **`moment(date, 'DD/MM/YYYY')` → `DateHelper.parse(date, 'DD/MM/YYYY')`**: `useAttendanceControl.js` (justo el caso que expuso el bug de `DateHelper.parse` de arriba).
  - **El más no-mecánico**: `getDaysDiffExcMonday` en `Utils.js` — el bucle original mutaba el objeto `moment` in-place (`from.add(1, 'days')`); como dayjs es inmutable, se reescribió como `from = DateHelper.add(from, 1, 'day')` con reasignación explícita en cada vuelta. También usaba `.isoWeekday() !== 7` (moment: lunes=1…domingo=7) para "no es domingo", traducido a `DateHelper.getWeekday(from) !== 0` (dayjs `.day()`: domingo=0). `useModalViewDetailPay.js` (que llama a esta función) tenía el mismo patrón de mutación in-place en `date2.add(...)`/`date2.subtract(...)` encadenados — reescrito con reasignación explícita en cada paso; de paso se eliminó una variable `date1` que quedó redundante (solo existía para evitar mutar `dateInit`, ya innecesaria con dayjs inmutable).
  Verificado: `eslint` sin errores nuevos relacionados (0 coincidencias de `moment`/`DateHelper`/`date1`/`date2` en el diff de lint), `vite build` (bundle de entrada bajó otros ~40 KB) + `npm test` (40 tests, incluye el nuevo de `DateHelper.parse`) limpios. En navegador con login real: pantalla de Permisos (`usePermissions.js`) mostrando datos reales de producción — columna "Time" con valores "1 Dias"/"2 Dias"/"2 Horas"/"4 Horas" calculados correctamente vía `DateHelper.diff` contra fechas reales del backend. Pantallas de Attendance Control y Resume Payroll (que hospedan a `useAttendanceControl.js` y `useModalViewDetailPay.js`) cargan sin errores de consola. El módulo Hotel Management no está habilitado para la marca usada en la verificación (`hiperlimpieza`), así que `useModalNewQuote.js`/`useModalAddRes.js`/`useModalInvoice.js` quedan verificados por lectura de código + el patrón idéntico ya confirmado en Permisos, no por prueba visual directa.)*
- [ ] Migrar los componentes de clase restantes a hooks. *(`dropzone/index.js` se borró por completo al consolidar librerías — ver bloque Medio. Parcial: `NotificationContainer.js` migrado a función + hooks (`useState`/`useEffect`, con cleanup que remueve el listener del `NotificationManager` al desmontar — mismo patrón que el `componentDidMount`/`componentWillUnmount` original, ahora sin el bug de timing porque `useEffect` corre después del commit por diseño). Se eliminó `PropTypes` siguiendo la convención ya usada en otros componentes función del repo (parámetros con default + `/* eslint-disable react/prop-types */`). Verificado: build + lint + `npm test` (39 tests) sin regresiones, y en navegador con login real: una notificación de error real (login con password incorrecto) se renderiza y descarta correctamente, sin warnings de React en consola. **Queda pendiente `Sidebar.jsx`** — deliberadamente fuera de este alcance: es la navegación principal de toda la app (596 líneas, `ReactDOM.findDOMNode`, manipulación directa del DOM vía `querySelector` para el link activo, HOC `withRouter` propio, listeners de resize/click), una reescritura real y no mecánica que amerita su propia tarea dedicada con verificación exhaustiva en navegador.)*
- [x] Reactivar `React.StrictMode` en `src/main.js` (está comentado). *(Resuelto. Verificado con login real contra el backend local (marca `hiperlimpieza`): dashboard (FullCalendar), formularios con ribbon (`Customers`, `Purchase Orders` — este último ya usa los hooks `usePurchaseFormLists`/`usePurchaseOrders` del refactor de `usePurchases.js`), y el `Sidebar.jsx` de clase (el único punto de esta lista que sigue sin migrar a hooks) se montan y doble-renderizan sin warnings nuevos. Comparado explícitamente contra el mismo flujo con StrictMode desactivado (`git stash` temporal) para aislar causa: un 401 y un `TypeError` en `pointSales/Content.js:713` (`forEach` sobre `undefined`) ya existían sin StrictMode — StrictMode solo los duplica (doble-invocación de efectos en dev, comportamiento esperado), no los origina. Ese bug de `pointSales` queda como hallazgo nuevo, no corregido acá (fuera de alcance de este ítem). Build (`vite build`) + `npm test` (39 tests) sin regresiones.)*
- [x] Limpiar imports `import React` innecesarios (428 archivos) — correr `eslint --fix` (57 auto-fixables) y continuar gradualmente. *(Los "57 auto-fixables" de la nota original resultaron ser otra cosa: comentarios `eslint-disable`/`eslint-disable-next-line` obsoletos (de la vieja config `.eslintrc.cjs`, referenciando reglas que `eslint.config.js` ya no habilita, ej. `react/destructuring-assignment`, `react/sort-comp`), no los imports de React — `eslint --fix` normal no toca `no-unused-vars` en absoluto (0 de los mensajes reales tenían `fix` disponible). Se descartó ese `--fix` amplio tras probarlo: dejaba artefactos sucios (líneas en blanco, y en un caso puntual de `Sidebar.jsx` convertía `{/* eslint-disable-next-line no-nested-ternary */}` en un `{ }` huérfano).
  El recuento real de imports de React sin usar dio **683 archivos** (no 428 — la base creció desde la auditoría original), todos con uno de dos patrones 100% mecánicos: `import React from 'react';` (eliminar la línea completa, 380 casos) o `import React, { x, y } from 'react';` (quitar solo `React,`, 303 casos) — confirmado con un script que extrajo la línea exacta de cada uno de los 683 vía el JSON de `eslint` antes de tocar nada; 0 casos no encajaron en los dos patrones. Aplicado con un codemod de una sola pasada (Node, no versionado). Verificado: `eslint` ya no reporta ningún `'React' is defined but never used` (bajó de 7427 a 6690 problemas totales, la diferencia exacta), `vite build` y `npm test` (39 tests) limpios, y navegador con login real confirmando que el `Sidebar.jsx` de clase (el archivo con más ediciones del lote) sigue renderizando y desplegando submenús con normalidad.)*
- [ ] Mejorar accesibilidad: ampliar `aria-*` en modales/tablas (casi inexistente hoy); agregar `role="button"`/`tabIndex`/manejo de teclado en `src/components/uploadFile/Content.jsx` (`<div onClick>` no accesible).
- [x] Agregar `sandbox` al `<iframe>` de `src/components/ViewPDF/ViewPdf.jsx`. *(Resuelto: `sandbox="allow-same-origin"`. De paso se encontró y arregló un bug visual real en el mismo archivo: un comentario `// embed-responsive-16by9` puesto directo como hijo de JSX (no envuelto en `{/* */}`) se renderizaba como texto literal en pantalla — visible en la captura de verificación. Verificado con login real contra el backend local: se buscó un empleado real, se abrió su "Entry Sheet" (mismo componente `ViewPdf`), confirmado el atributo `sandbox` presente en el iframe real y el texto suelto ya no aparece.)*
- [x] Optimizar imagen de fondo del login (`src/assets/img/login/back-auth.jpg`, 1.4 MB) — comprimir/convertir a WebP. *(La nota original apuntaba al archivo equivocado: `src/assets/img/login/back-auth.jpg` (1.4 MB) resultó ser un duplicado muerto sin ningún importador — el SCSS referencia la imagen por ruta absoluta (`url('/assets/img/login/back-auth.jpg')`), que en Vite resuelve contra `public/`, no contra `src/`. El archivo realmente servido era `public/assets/img/login/back-auth.jpg`, de **2.99 MB** (el doble de lo que decía la nota), 3840×2160 — confirmado comparando tamaños y decodificando las dimensions JPEG de ambos antes de tocar nada.
  Convertido con `sharp` (instalado temporalmente en el scratchpad, no como dependencia del proyecto) a WebP, redimensionado a 2560px de ancho (sigue cubriendo cómodamente cualquier viewport real vía `background-size: cover`, sin necesidad de 4K para un fondo decorativo) y calidad 75 — probé 65/75/80 y elegí 75 por ser visualmente indistinguible del original en captura de pantalla. Resultado: **2.99 MB → 213 KB (~93% menos)**. Actualizada la única referencia real en `src/assets/sass/core/base.scss`; borrados el `.jpg` viejo servido y el duplicado muerto en `src/`. No se agregó fallback a JPG — WebP tiene soporte universal en los navegadores modernos que ya requiere React 19/Vite 6.
  (De paso se encontró que `public/assets/sass/core/base.scss` es una copia duplicada y no usada del mismo archivo — Vite solo compila `src/assets/sass/**`, nada en `index.html` ni en el bundle referencia sass servido desde `public/`. Quedó con su URL vieja sin actualizar porque nunca se ejecuta; no se tocó para no ampliar el alcance de este ítem — si se decide limpiar esa carpeta `public/assets/sass/` completa, es una tarea aparte.)
  Verificado: `vite build` (el `.webp` queda en `dist/assets/img/login/`, sin ningún `.jpg`) + `npm test` (40 tests) limpios, y en navegador: la pantalla de login carga `back-auth.webp` (200, 213 KB) sin errores de consola, visualmente idéntica al original.)*
- [x] Fijar y documentar una convención de alias de import (`@Helpers` vs `@/helpers`, hoy mezclados) y de extensión de archivo (`.js` vs `.jsx` para componentes con JSX). *(Ambas mitades resueltas con migraciones mecánicas completas, no solo documentación — a pedido explícito del usuario ("los que amerite convertir a jsx conviertelos").
  **Extensión de archivo**: se detectaron los `.js` que realmente contienen JSX con `esbuild.transformSync` (falla con `loader: 'js'`, funciona con `loader: 'jsx'`) en vez de adivinar por contenido con regex — 446 de 698 archivos `.js` calificaron, confirmado sin colisiones de nombre (`Xxx.jsx` ya existente) y sin ningún import local con extensión `.js` explícita en todo el repo (los ~16 falsos positivos eran todos el paquete npm `chart.js`), así que renombrar no requería tocar ninguna línea de import — pura operación de archivo. Excluido `src/main.js` (referenciado por ruta literal en `index.html`, documentado como excepción en `CLAUDE.md`). Incluido `src/App.js` → `App.jsx` sin colisión (el `App.jsx` muerto de create-vite ya se había borrado en un ítem anterior). De paso se encontró y arregló un bug real preexistente en código muerto: `src/components/optionCards/index.js` tenía una coma suelta (`{ , FormGroup, ... }`) que rompía el parseo — nunca se detectó porque el archivo no tiene importadores (0 usos), así que Vite/esbuild nunca llegó a parsearlo; se corrigió (y se sacó el import de `FormGroup`/`Input`/`Label`/`React` que tampoco se usaban) antes de convertirlo.
  **Convención de alias**: se midió el uso real antes de decidir — el estilo `@/minúscula` (ej. `@/helpers`) era mayoritario (~1500 líneas de import) contra los alias con nombre (`@Helpers`, etc., ~900 líneas), excepto `@Hooks` que ya era 100% del estilo nombrado. El usuario eligió estandarizar en la dirección minoritaria (alias con nombre) de todos modos. Migrados con un codemod de reemplazo de string dentro de comillas (`'@/helpers/` → `'@Helpers/`, mismo patrón para `components`, `constants`, `containers`, `redux`, `views`, más el caso especial `@/hooks` sin slash final que importa el barrel `index.js`): 789 archivos, 2567 reemplazos totales. Confirmado con grep que no queda ningún `@/` en todo `src/` después de la migración — incluye la constatación de que el alias raíz `@` (→ `./src`) quedó sin ningún uso real, documentado en `CLAUDE.md` como tal (se dejó definido en `jsconfig.json`/`vite.config.js` por si se necesita para carpetas sin alias propio, ej. `assets`, `lang`).
  **Bug real preexistente encontrado (no introducido en esta sesión) durante la migración de alias**: 53 archivos tenían el comentario `// eslint-disable-next-line react-hooks/exhaustive-deps` corrompido a `react-@/hooks/exhaustive-deps` — confirmado con `git show HEAD:...` que la corrupción YA estaba en el archivo antes de cualquier cambio de esta sesión (típicamente el resultado de un find-replace ingenuo de "hooks" en algún momento anterior del proyecto). Ese typo hacía que ESLint no reconociera el nombre de la regla y el `eslint-disable` fuera un no-op silencioso — corregido a `react-hooks/exhaustive-deps` en los 53 archivos (bajó el conteo de warnings de lint en la misma medida).
  Verificado: `npm test` (40 tests) y `vite build` (mismo tamaño de bundle, 702.60 kB de entrada) sin regresiones en ambos pasos (rename de extensión y migración de alias, cada uno verificado por separado). `eslint` sin errores de parseo ni módulos rotos (7427→6689→6700→6593 problemas totales a través de toda la sesión, sin picos que indiquen archivos rotos). En navegador con login real: dashboard, `Customers`, `Purchase Orders`, `Permissions` (con datos reales de producción), `Users` (settings) y `Ledger Accounts` cargan sin errores de consola nuevos — un par de "pantalla en blanco" durante la verificación resultaron ser arranques en frío del dev server (primera compilación de cientos de módulos recién renombrados/editados) tardando más de los 2.5-4s de espera inicial del script, no una regresión real; confirmado reproduciendo con espera de 9s.)*
- [x] Corregir typos en nombres de archivo: `useChashBoxes.js` → `useCashBoxes.js`, `redux/contants.js` → `redux/constants.js`. *(Resuelto con `git mv` (preserva historial) + actualización de todos los call sites: `useChashBoxes.js` tenía un solo importador (`cashBoxes/Content.js`), y de paso se renombró también el nombre interno de la función/export (`useChashBoxes` → `useCashBoxes`), no solo el archivo. `redux/contants.js` tenía 7 importadores (`auth/{actions,reducer,saga}.js`, `menu/{actions,reducer}.js`, `settings/{actions,reducer}.js`), todos con `from '../contants'` → `from '../constants'`. Confirmado con grep que no queda ninguna referencia a `contants`/`ChashBoxes` en todo `src/`. Verificado con `npm test` (40 tests) y `vite build` limpios — sin verificación en navegador esta vez, a pedido explícito del usuario de confiar en test+build para este cambio.)*
- [x] Plan de actualización incremental de dependencias desactualizadas (Vite 6→8, react-intl 7→10, sass, etc.), con testing manual dado que no hay tests automatizados todavía. *(Investigado y planificado — no ejecutado en esta sesión, el ítem pedía el plan, no el upgrade en sí. Recuento real con `npm outdated` + medición de huella real de uso (`grep` de cada librería en `src/`) para calibrar riesgo por lo que realmente toca, no solo por cuántas versiones mayores de diferencia hay. Ordenado en 3 tandas, de menor a mayor riesgo/esfuerzo:
  **Tanda 1 — bajo riesgo, hacerlo primero** (bumps menores o de huella mínima): `react-router-dom` 7.17→7.18 (patch dentro de la misma mayor), `uuid` 13→14, `@fullcalendar/daygrid` 6.1.20→6.1.21 (patch), `eslint-plugin-react-refresh` 0.4.26→0.5.3. `dotenv`/`fs-extra`/`archiver`/`basic-ftp` tienen 0 uso confirmado en el repo (ver ítem de auditoría de `package.json` más arriba) — probablemente los usa `post-build.js` (gitignoreado, ausente del working tree); no se pueden verificar sin ese archivo, así que actualizarlos es responsabilidad de quien tenga ese script.
  ***Tanda 1 — ejecutada.*** `react-router-dom` y `@fullcalendar/daygrid` subieron con `npm update` (ya cabían en el rango `^` existente en `package.json`, solo cambió `package-lock.json`); `uuid` y `eslint-plugin-react-refresh` con `npm install <pkg>@latest` (cruzan de mayor/rango 0.x, sí tocan `package.json`). Único uso real de `uuid` en el repo es `v4` en `UuIdGenerator.js`, API sin cambios entre mayores. `npm audit` sigue mostrando solo la vulnerabilidad ya conocida de `xlsx` (sin fix disponible, ver ítem Crítico) — nada nuevo. El lint subió de 6593 a 6765 problemas, pero el delta completo (+172) es la regla `react-refresh/only-export-components`, más estricta en la v0.5 del plugin — solo afecta Fast Refresh en dev, no rompe build ni runtime; no se corrigió en bulk (mismo criterio que el resto del backlog de lint). Verificado: `npm test` (40 tests) + `vite build` (mismo tamaño de bundle) limpios, y en navegador con login real: dashboard con calendario y sidebar funcionando con el `react-router-dom` nuevo.
  **Tanda 2 — riesgo moderado, huella acotada y bien delimitada**: `react-datepicker` 7→9 (2 mayores, pero solo 2 archivos reales lo usan: `dateCalendar`/`dateTimeCalendar`, ya consolidados en un ítem anterior). `react-dropzone` 14→19 (5 mayores, pero 1 solo archivo: `UploadImages.jsx`). `@fullcalendar/react` 6→7 (1 mayor, 2 archivos). `eslint` 9→10 + `eslint-plugin-react-hooks` 5→7 (solo tooling de desarrollo, no runtime — bajo riesgo real, pero puede sumar hallazgos nuevos al backlog de lint de ~6500 ítems ya existente; conviene medir el delta antes/después igual que se hizo con otros cambios de esta sesión).
  ***Tanda 2 — ejecutada parcialmente, con 2 hallazgos reales que cambiaron el plan:***
  - **`react-datepicker`**: subido a **8.10.0**, no a 9.x como decía el plan original — investigado antes de instalar: la v9 agrega una peer dependency nueva (`date-fns-tz@^3.0.0`) que el proyecto no tiene instalada; la v8 (última de esa mayor) no la requiere y sigue siendo un salto real desde el 7.6.0 anterior. Verificado con `npm test` + `vite build` limpios, y en navegador con login real: el calendario de `DateCalendar` (pantalla Customers, campo "Date Admission") abre y navega meses correctamente.
  - **`react-dropzone`**: subido a **19.0.2** como estaba planeado — el único uso real (`useDropzone` en `UploadImages.jsx`) ya usaba la forma moderna de la API (`getRootProps`/`getInputProps`/`accept` como objeto por MIME type), estable entre v14 y v19. Verificado con build limpio y en navegador (pantalla Products Catalog, módulo que importa `UploadImages` vía `ModalImages`) sin errores de consola.
  - **`@fullcalendar/react` — NO se subió, hallazgo real que lo bloquea**: `@fullcalendar/react@7` es una versión estable publicada, pero **`@fullcalendar/daygrid@7` todavía no tiene release estable** (solo `7.0.0-beta.0` a `7.0.0-rc.0` en el registro de npm) — subir solo el paquete `react` mientras `daygrid` se queda en v6 rompe la compatibilidad entre paquetes de FullCalendar (todos los paquetes del ecosistema deben coincidir en mayor). Además `@fullcalendar/react@7` agrega una peer dependency nueva (`temporal-polyfill@^1.0.1`) que tampoco está instalada. Queda pendiente hasta que `@fullcalendar/daygrid` tenga una v7 estable publicada.
  - **`eslint` — se intentó subir a 10.x y se revirtió**: bloqueado por un hallazgo real, no una decisión de cautela — `eslint-plugin-react` (incluso su última versión, `7.37.5`) declara `peerDependencies.eslint: "^3 || ^4 || ^5 || ^6 || ^7 || ^8 || ^9.7"`, sin soporte para ESLint 10 todavía. De paso se encontró que `eslint.config.js` importa `@eslint/js`, que nunca fue una dependencia declarada en `package.json` — dependía del hoisting transitivo de `eslint@9` en `node_modules`; con `eslint@10` ese hoisting cambió y el import dejó de resolverse (`ERR_MODULE_NOT_FOUND`), rompiendo el lint por completo. Revertido a `eslint@9.39.5` (la última patch de la mayor 9, antes estaba en `^9.29.0`). Si en el futuro se sube a ESLint 10, hay que agregar `@eslint/js` como devDependency explícita además de esperar a que `eslint-plugin-react` lo soporte.
  - **`eslint-plugin-react-hooks`** sí se pudo subir a **7.1.1** (su rango de peer `eslint` acepta tanto `^9.0.0` como `^10.0.0`, así que no depende de la resolución de `eslint` en sí) — verificado que el lint corre igual que antes (6765 problemas, sin cambios respecto a después de la Tanda 1).
  Verificado en conjunto: `npm test` (40 tests) + `vite build` (mismo tamaño de bundle) + `npm audit` (solo la vulnerabilidad ya conocida de `xlsx`) limpios.
  **Tanda 3 — mayor riesgo/esfuerzo, requieren atención dedicada**:
  - **`sass`** 1.69→1.101: no es solo un bump de versión — los 24 archivos `.scss` del proyecto usan exclusivamente la sintaxis `@import` legacy (0 usan `@use`/`@forward`, la API moderna de Dart Sass). Dart Sass viene deprecando `@import` desde hace varias versiones camino a removerlo; subir la versión sin modernizar la sintaxis primero va a inflar la salida del build con warnings de deprecación (o directamente romper, según qué tan lejos se salte). Recomendado: correr `sass-migrator` (herramienta oficial del equipo de Dart Sass) para migrar los 24 archivos a `@use`/`@forward` como paso previo o en el mismo cambio, no después.
    ***Ejecutado, con un hallazgo que corrigió la evaluación de riesgo original***: subido a **1.101.0**. Compilando un archivo con el CLI de `sass` directo (`npx sass archivo.scss out.css`) confirmé que la v1.101 sí emite el warning de deprecación de `@import` en cascada por cada archivo de la cadena — pero **el pipeline de Vite lo silencia por completo**: `vite build` corrió limpio, sin ni un warning de deprecación en la salida. Esto significa que subir la versión ahora es seguro (no rompe ni ensucia el build actual), aunque la deuda de fondo (migrar a `@use`/`@forward` con `sass-migrator` antes de que Dart Sass 3.0.0 remueva `@import` del todo) sigue en pie como tarea aparte, ya no urgente. Verificado con `vite build` limpio y comparación visual en navegador (dashboard con los 10 temas de color no se tocó, pero el tema activo por defecto se ve pixel-a-pixel igual al baseline anterior).
  - **`react-intl`** 7→10 (3 mayores): la huella real es mucho más chica de lo que sugiere el número de mayores de diferencia — 455 archivos usan el helper `IntlMessages`, pero **todos** pasan por un único punto de indirección (`src/helpers/IntlMessages.jsx` + `Utils.js`), así que no hace falta tocar 455 archivos. Los puntos de contacto reales con la librería son solo 7: `App.jsx` (setup de `IntlProvider`), `helpers/IntlMessages.jsx`, `helpers/Utils.js`, `lang/index.js` (carga de datos de locale), y 3 usos puntuales de hooks de `react-intl` (`RecentPost.jsx`, `uploadFile/index.jsx`, `Topnav.jsx`). Verificar manualmente el cambio de idioma EN↔ES (ya es parte del checklist de verificación de otros ítems de esta sesión) cubre la mayoría del riesgo real.
    ***Ejecutado, con un breaking change real confirmado y arreglado***: subido a **10.1.18** (no la primera 10.0.0 — las versiones 10.0.0–10.1.13 fijaban `peerDependencies.react` a exactamente `"19"`, sin rango; recién en 10.1.14 se relajó a `>=18.0.0`, mucho más seguro para no quedar pegado a un patch exacto de React). El build rompió al primer intento: `injectIntl` (el HOC legacy de la API de clases) fue removido por completo en v10 — `"injectIntl" is not exported by react-intl`. Encontrado en 3 de los 7 puntos de contacto: `helpers/IntlMessages.jsx`, `components/common/RecentPost.jsx`, `containers/navs/Topnav.jsx`. Investigado cada uno antes de tocar nada: **ninguno de los 3 usaba realmente el prop `intl` inyectado** — `IntlMessages.jsx` envolvía `<FormattedMessage>` (que no necesita `injectIntl` para nada), `RecentPost.jsx` no lo usaba en absoluto, y `Topnav.jsx` destructuraba `const { messages } = intl` pero esa variable `messages` no se leía en ningún lado del archivo. Los 3 se simplificaron sacando el HOC (y el prop `intl`/import correspondiente) en vez de migrarlos a `useIntl()`, ya que no había nada real que migrar — código muerto que react-intl v10 obligó a limpiar. Verificado con `npm test` (40 tests) + `vite build` limpio (bundle de entrada bajó ~14 KB, react-intl v10 sacó código de compatibilidad legacy) + `eslint` sin hallazgos nuevos, y en navegador con login real: cambio de idioma EN↔ES funciona (sidebar, dashboard y el formulario completo de Customers — que usa la forma de componente `<IntlMessages id="..." />` en sus ~20 labels — se traducen correctamente), sin errores de consola nuevos.
  - **`vite`** 6→8 (2 mayores) + `@vitejs/plugin-react` 4→6: la más riesgosa por ser la herramienta de build en sí, con radio de impacto sobre toda la app. Vite 7 subió el mínimo de Node requerido (20.19+/22.12+) — la CI (`ci.yml`) usa `actions/setup-node@v4` con `node-version: 20` sin pin de patch, que en 2026 resuelve a una versión >20.19, así que debería estar bien, pero conviene confirmarlo explícitamente antes de subir. Además, ahora que la convención de extensión de archivo quedó fijada (`.jsx` para JSX real, ver ítem de arriba), el hack de `esbuild.loader = "jsx"` + `include: [...]` en `vite.config.js` para JSX-en-`.js` quedó necesario solo para `src/main.js` (la única excepción documentada) — una futura migración de Vite es también la oportunidad natural para simplificar esa config. Dejar esta tanda para el final, después de ganar confianza con las tandas 1 y 2, y con su propia verificación manual exhaustiva en navegador (login, todos los módulos, build de producción) dado que no hay cobertura de tests automatizados para UI todavía.
    ***Ejecutado, con 3 breaking changes reales confirmados y arreglados*** — subido a `vite@8.1.5` + `@vitejs/plugin-react@6.0.3` juntos (`@vitejs/plugin-react@6` exige `vite@^8`, no se pueden subir por separado). Node local (24.17.0) y las nuevas peer dependencies opcionales del plugin (`@rolldown/plugin-babel`, `babel-plugin-react-compiler`, marcadas `optional: true`) no dieron problema.
    1. **Vite 8 cambió el pipeline de build de esbuild a Rolldown/oxc**, y el hack de `esbuild.loader = "jsx"` para JSX-en-`.js` dejó de funcionar por completo — rompió exactamente donde se esperaba: `src/main.js` (la única excepción documentada de la convención de extensión). Solución, tal como estaba anticipado en este mismo ítem del plan: se renombró `src/main.js` → `src/main.jsx`, se actualizó la referencia literal en `index.html` (`<script src="/src/main.jsx">`), y se borró por completo el bloque `esbuild`/`optimizeDeps.esbuildOptions` de `vite.config.js` (junto con una función `rollupPlugin`/`sourceJSPattern` que ya estaba muerta desde antes — nunca se agregaba a `plugins: []`, y además referenciaba un `esbuild` que ni siquiera estaba importado en el archivo, hubiera tirado `ReferenceError` si alguna vez se hubiera invocado). Re-confirmado con el mismo método de detección de la migración de extensión (`esbuild.transformSync`) que 0 archivos `.js` en todo `src/` contienen JSX real después del rename.
    2. **La nueva versión de `sass` (bumpeada en un paso anterior) falla al compilar `src/assets/sass/views/landing-page.scss` bajo el pipeline CSS de Vite 8**, un error de sintaxis en una construcción de string SVG data-URI (`url("...") + encodecolor(...)`) que el CLI de `sass` directo compila sin problema — el bug está en cómo Vite 8/Rolldown invoca al compilador de sass, no en el sass en sí. Investigado antes de tocar nada: `landing-page.scss` no tiene **ningún** consumidor real (0 coincidencias de sus clases en todo el código JSX/JS) — es CSS muerto heredado de la plantilla admin original, nunca usado por ninguna pantalla real de este ERP. Se sacó su `@import` de `_05_views.scss` en vez de arreglar el string frágil, la opción de menor riesgo dado que es código sin uso.
    3. **El dev server (`npm run dev`, no `vite build`) queda inundado de warnings de deprecación de sass** (`@import`, `color-functions`, `global-builtin` — cientos de líneas por cada archivo de tema, 20 temas) — a diferencia de `vite build`, que los silencia, `vite dev` los imprime todos, y en un momento esto pareció causar timeouts de navegación en la verificación (después se confirmó que el timeout real era por otra causa — ver abajo — pero el ruido en la terminal ya era un problema real de experiencia de desarrollo por sí solo). Arreglado agregando `css.preprocessorOptions.scss.silenceDeprecations: ['import', 'color-functions', 'global-builtin']` a `vite.config.js`, con un comentario explicando que la migración real a `@use`/`@forward` sigue pendiente (ver punto de `sass` más arriba) y esto es solo para no ensuciar la terminal mientras tanto.
    **Hallazgo aparte, no un bug de Vite 8 pero descubierto durante el barrido de regresión más amplio que ameritó esta tanda**: la pantalla `accounting/settings/ledgerAccounts` crasheaba por completo (`ReferenceError: InputMask is not defined`) — el import de `react-input-mask` estaba comentado (`// import InputMask from 'react-input-mask'`) pero la línea `tag={InputMask}` se quedó sin comentar. Investigado antes de arreglar: hay **7 archivos más** en el repo con el mismo patrón de `react-input-mask` removido (`otherPurchases/Content.jsx`, `purchaseOrders/ModalProcessOrder.jsx`, `purchases/FormPurchase.jsx`, `fuelPurchases/InvoiceDetail.jsx`, `providers/ModalBillingData.jsx`, `taxDocuments/Detail.jsx`), y en todos ellos el `tag={ReactInputMask}` correspondiente SÍ está comentado — `ledgerAccounts` fue el único que se quedó a medias. Existe un `InputMask.jsx` local moderno (`@Components/inputFields`, envolviendo `@react-input/mask`) pensado como reemplazo, pero ningún archivo lo usa todavía — migrar los 8 archivos a esa API real (con su propia sintaxis de `mask`/`replacement`, distinta a la vieja `mask`/`maskChar`) es una tarea de UI diferida aparte, no incluida acá. Se corrigió `ledgerAccounts` comentando `tag={InputMask}`, igual que los otros 7, restaurando la pantalla completa (antes: pantalla en blanco atrapada por el `ErrorBoundary`; después: tabla real de 42 páginas de cuentas contables funcionando) al costo de no tener la máscara visual en el campo "Code" — mismo estado no funcional que ya tenían los otros 7.
    Verificado extensamente: `npm test` (40 tests) + `vite build` (compiló en 8.7s vs ~20-30s antes — Rolldown es Rust, no JS — y el bundle de entrada bajó a 624.80 kB) + `eslint` (6759 problemas, sin cambios) + `npm audit` (solo la vulnerabilidad ya conocida de `xlsx`) limpios. En navegador con login real, barrido completo por 7 pantallas de módulos distintos (Dashboard, Customers, Point Sales, Purchase Orders, Permissions con datos reales, Products Catalog, Ledger Accounts): 0 errores nuevos de consola en todas — solo los ya documentados (401 preexistente en Customers, warning de input controlado preexistente en Point Sales, warning de `maskChar` en Ledger Accounts explicado arriba).
- [ ] Módulos enteros huérfanos sin ruta ni importadores vivos, hallados al consolidar librerías: `src/views/app/production/**` + `src/views/app/index.js` (router alternativo muerto que los referencia), y `src/views/app/start/**` (`start.jsx`, `ModalViewProject.jsx`). Ambos quedaron con imports colgantes tras borrar `dropzone/index.js` y desinstalar `react-big-calendar` respectivamente — inofensivo porque nada los importa desde el grafo de módulos real, pero conviene decidir si se recuperan (parece funcionalidad de "production"/proyectos a medio terminar) o se borran del todo.
  **Investigado sin borrar nada — pendiente decisión de negocio/equipo:**
  - `production/**` (settings: tipos de orden, encargados, destinos, tipos de producto, materia prima; process: órdenes de trabajo, cargos) es un módulo bastante completo, no un stub. Sus componentes `Content.jsx` ya usan hooks modernos (`useNavigate`), y los endpoints de backend que consume (`prodProjects`, `prodResponsibles`, `prodOrderTypes`, `prodDestinations`, `prodProductTypes`, `prodSteps`, `prodRMStocks`, `prodProducts`) responden **401** contra el backend local (protegidos por auth, no 404) — el backend sigue sirviéndolos. Lo único roto es `src/views/app/index.js`, el router que los conecta: usa APIs de `react-router-dom` v5 (`Switch`, `withRouter`) que no existen en la v7 instalada — recuperarlo requiere reescribir ese router, no solo volver a importarlo en el árbol de rutas actual.
  - `start/**` (`start.jsx`, `ModalViewProject.jsx`, vista de calendario de proyectos de producción) está genuinamente más roto: importa `react-big-calendar` (ya desinstalado) y usa `onTitleEdit`/`onBreadcrumbEdit`/`adminRoot` sin importarlos — no compilaría tal cual está, no es solo un import colgante inofensivo.
  Con esto en la mesa, la decisión (recuperar `production/` reescribiendo su router, o borrar ambos del todo) queda para cuando el negocio confirme si el módulo de producción/manufactura se va a retomar.
- [x] Warning de React "Can't perform a React state update on a component that hasn't mounted yet", visto en `meetingNotices`. *(Resuelto: investigado con login real contra el backend local. El warning NO tenía relación con `DetailMeeting.jsx` ni con `meetingNotices` — la traza de stack (capturada interceptando `console.error` en el navegador) apuntó a `NotificationContainer.js`: se suscribía a `NotificationManager` (event emitter) en el `constructor`, un anti-patrón clásico de React — si un evento de notificación se emite entre la construcción y el commit/montaje, `handleStoreChange` llama `setState` sobre una instancia que React todavía no marcó como montada. Arreglado moviendo `NotificationManager.addChangeListener(...)` del `constructor` a `componentDidMount`. Verificado: el warning se reproducía consistentemente al guardar un aviso de reunión (dispara una notificación); tras el fix, 0 warnings en 3 corridas seguidas de la misma acción. De paso, mientras se reproducía el warning se encontró y arregló un bug real en `useMeetingNotices.js`: `fnSaveDocument` llamaba `fnPrintPdf({id})` usando el `id` del closure (siempre `0` al crear), en vez de `resp.data.id` (el id real devuelto por el POST) — causaba un 500 real del backend al intentar exportar el PDF de un registro con id 0, cada vez que se creaba un aviso nuevo. Mismo patrón que `fnPrintInvoice(resp.data.id)` en `ModalGenerateInvoice.jsx`. Build + lint + `npm test` (39 tests) sin regresiones.)*
- [ ] Warning nuevo encontrado con datos reales (login real contra el backend local, pantalla de empleados): `` `value` prop on `%s` should not be null `` + "A component is changing a controlled input to be uncontrolled" — sugiere que algún campo del formulario de empleado recibe `null` desde el backend en vez de `''`/`undefined` para algún registro real. No investigado a fondo, no bloquea nada visualmente.
- [x] Bug nuevo encontrado con datos reales al verificar `React.StrictMode` (login real, marca `hiperlimpieza`): `billing/process/pointSales/Content.js:713` tira `TypeError: Cannot read properties of undefined (reading 'forEach')` al abrir la pantalla — la respuesta de `core.js:113` (dentro de un callback de `request`) llega sin el campo que se itera. *(Resuelto: el código asumía que `GET admin/paymentTypes` devuelve cada `paymentType` con un array anidado `paymentTypeDetails`, e iteraba `item.paymentTypeDetails.forEach(...)` sin guard. Confirmado contra el backend real (llamada directa al endpoint con el usuario de prueba) que **los 8 registros** que devuelve la API para esta marca tienen `paymentTypeDetails: undefined` — no es un caso raro, es la forma real de la API completa; el `forEach` fallaba siempre, en el primer item, cada vez que se abría la pantalla. Se confirmó además que ningún otro consumidor del mismo endpoint en todo el repo (10 archivos: `usePurchaseFormLists.js`, `usePaymentMethods.js`, `useReservation.jsx`, etc.) espera esa estructura anidada — todos hacen un `.map()` plano tratando cada `paymentType` como una opción individual. Reescrito para seguir ese mismo patrón, ajustando el shape a lo que realmente consumen `ModalPayment.jsx`/`EditValuesTable` en este archivo (`id`, `name`, `usageType`, `value` inicializado en `0` — no el shape `{label,value}` de react-select que usan los otros 10 consumidores, que es para un dropdown distinto). Antes del fix esto dejaba, además del crash, la lista de formas de pago del checkout permanentemente vacía incluso si alguien hubiera agregado solo un guard defensivo sin corregir el shape. Verificado: `npm test` (40 tests) + `eslint` sin nuevos hallazgos, y en navegador con login real: la pantalla que antes quedaba con el loader trabado ahora renderiza el formulario completo (ribbon, cliente, tipo de venta, agregar producto, detalle de factura) sin el `TypeError` en consola.)*

---

*Generado el 2026-07-17 a partir de una auditoría de dependencias, arquitectura y seguridad/rendimiento/accesibilidad del proyecto.*
